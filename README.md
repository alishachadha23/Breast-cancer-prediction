# Breast-cancer-prediction
the seven models used are:
Logistic Regression
Decision Tree
Random Forest
Gradient Boosting
AdaBoost
Support Vector Machine (SVM)
K-Nearest Neighbors (KNN)
A comparison table showing Accuracy, ROC-AUC, and 5-fold CV score for all 7 models
4 visualizations: accuracy bar chart, ROC curves, CV scores, and a confusion matrix for the best model
A detailed classification report (precision, recall, F1) for the top-performing model
